# Zależności silnika tłumaczeń. Nie wchodzą do obrazu `builder` —
# codzienny build ma zostać lekki. Patrz Dockerfile.translator.

# To samo, co w builderze — skrypty localize/eksport dzielą kod
psycopg[binary]==3.2.3
lxml==5.3.0

# EN i DE: argostranslate (silnik + pakiety językowe pobierane osobno)
argostranslate==1.9.6

# AR: lokalny NLLB uruchamiany przez ctranslate2 w int8.
# NIE schodzić poniżej 4.8 — wersje do 4.5 mają w dołączonej bibliotece
# PT_GNU_STACK oznaczony jako RWX, czego jądro WSL2 odmawia załadować
# ("cannot enable executable stack as shared object requires").
ctranslate2==4.8.1
# NIE schodzić poniżej 4.56 — konwerter ctranslate2 przekazuje `dtype=`,
# a starsze transformers znają tylko `torch_dtype` i konwersja modelu NLLB
# wywala się na "unexpected keyword argument 'dtype'".
# Nie podnosić też do 5.x bez sprawdzenia — to zmiana major.
transformers==4.56.2
sentencepiece==0.2.0

# Potrzebny wyłącznie do JEDNORAZOWEJ konwersji modelu NLLB na format
# ctranslate2. Wariant CPU — patrz --extra-index-url w Dockerfile.translator.
torch==2.5.1
